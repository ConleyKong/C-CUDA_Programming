排序算法衡量的主要因素就是排序算法的时间空间复杂度。
因此在理解各种算法的思想时，注意考虑它们的时空复杂度。
♢有几种复杂度为O(N^2)的算法：
    插入排序-based排序算法
    希尔排序，复杂度就是O（N^2）而且非常常用。
♢有几种复杂度为O(N log N)的算法：
    快，堆等
♢任何通用的排序算法军需要Ω（NlogN）次比较。

稳定性时衡量排序算法的一个不是很突出的因素。所谓稳定性指的就是
某两个值相同的元素，在排序完成之后它们的相对位置是否发生改变。
